{
  "name": "@cmdb/data-mapper",
  "version": "2.0.0",
  "description": "Visual transformation engine for no-code data mapping",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@cmdb/common": "file:../common",
    "@cmdb/database": "file:../database"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
