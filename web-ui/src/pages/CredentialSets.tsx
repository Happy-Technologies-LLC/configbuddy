import React from 'react';
import { CredentialSetList } from '@/components/credentials/CredentialSetList';

export const CredentialSets: React.FC = () => {
  return <CredentialSetList />;
};

export default CredentialSets;
